# disable Spring Data JDBC's transitive JDBC driver autoconfiguration because it conflicts with
# Testcontainers/DynamicPropertySource value injection
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
# set database schema to be entirely driven by migrations
spring.test.database.replace=none
